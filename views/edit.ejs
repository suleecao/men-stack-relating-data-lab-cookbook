<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" /> 
    <title>Editing <%= food.food %></title>
</head>
<body>
    <%- include('./partials/_navbar.ejs') %>
    <h1>Edit <%= food.food %></h1>
    <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=PUT" method="POST">
        <label for="food">Food Item:</label>
        <input type="text" id="food" name="food" value="<%= food.food %>" required>
        <button type="submit">Update Pantry</button>
    </form>
    <form action="/users/<%= user._id %>/foods/<%= food._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete</button>
    </form>
    <a href="/users/<%= user._id %>/foods">Back to Pantry</a>
</body>
</html>